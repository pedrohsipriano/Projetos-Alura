let listaGenerica = [];

let linguagemDeProgramacao = ['JavaScript','C','C++', 'Kotlin', 'Python'];

linguagemDeProgramacao.push = ['Java', 'Ruby', 'GoLang'];

let listaNome = ["pedro", "giovanna", "gabriel"];
listaNome[0]; // para exibir apenas o primeiro elemento
listaNome[1]; // para exibir apenas o segundo elemento
listaNome[(listaNome.length) - 1]; // para exibir apenas o ultimo elemento